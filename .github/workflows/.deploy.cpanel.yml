on: 
  push:
    branch:
      - master
      
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.1
      with:
        server: ${{secrets.FRP_SERVER}}
        username: ${{secrets.FRP_USERNAME}}
        password: ${{secrets.FRP_PASSWORD}}

